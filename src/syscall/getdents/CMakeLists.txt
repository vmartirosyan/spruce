cmake_minimum_required(VERSION 2.6)

message("Configuring getdents syscall checker module.")

project (Spruce)

include_directories(${CMAKE_CURRENT_BINARY_DIR})

#ADD_LIBRARY(getdents STATIC getdents.cpp)
