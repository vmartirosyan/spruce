[group]
	# Name and return type of the target function
	function.name = write_one_page
	returnType = int

	# Names and types of the arguments of the target function
	arg.type = struct page *
	arg.name = page
	
	arg.type = int
	arg.name = wait
	
	# Fault Simulation
	# TODO: Check code
	fpoint.fault_code =	ret_val = -EIO;
#######################################################################
